#ifndef NumCellInst_hpp
#define NumCellInst_hpp

#include <stdlib.h>
#include <stdio.h>

void printCellInstances();
void readNumCellInst(char* line, size_t len, FILE* file);

#endif /* NumCellInst_hpp */
